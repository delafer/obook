pluginManagement {
  repositories {
    maven { url 'https://repo.spring.io/milestone' }
    maven { url 'https://repo.spring.io/snapshot' }
    gradlePluginPortal()
  }
}

rootProject.name = 'ebook-indexer-parent'

//include 'frontend', 'shared', 'backend'
include 'shared', 'backend'
