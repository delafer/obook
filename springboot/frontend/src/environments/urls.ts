import { environment } from '$environment/environment';

export class Urls {
  public static restUrl = `${environment.restUrl}`;
  public static frontendUrl = ``;
  public static wsUrl = `${environment.wsUrl}`;
}
